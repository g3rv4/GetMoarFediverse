name: build
on:
  pull_request:
    types: [ synchronize ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build things
        shell: pwsh
        run: |
          docker build .
